{
  "name": "ship-it",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "ship-it": "./dist/index.js"
  },
  "scripts": {
    "dev": "bun run src/index.tsx",
    "build": "bun build src/index.tsx --outdir dist --target node",
    "compile": "bun build src/index.tsx --compile --outfile ship-it",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "ink": "^5.0.1",
    "ink-text-input": "^6.0.0",
    "ink-select-input": "^6.0.0",
    "ink-spinner": "^5.0.0",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "@types/react": "^18.3.12",
    "typescript": "^5.7.2"
  }
}
